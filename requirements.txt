beautifulsoup4>=4.12.2  # Allow any version >= 4.12.2
bs4==0.0.1              # Remains the same
certifi>=2023.5.7       # Allow any version >= 2023.5.7
chardet>=5.1.0          # Allow any version >= 5.1.0
click>=8.1.3            # Allow any version >= 8.1.3
Flask>=2.3.2            # Allow any version >= 2.3.2
Flask-Cors>=4.0.0       # Allow any version >= 4.0.0
gunicorn>=20.1.0        # Allow any version >= 20.1.0
idna>=3.4               # Allow any version >= 3.4
itsdangerous>=2.1.2     # Allow any version >= 2.1.2
Jinja2>=3.1.2           # Allow any version >= 3.1.2
MarkupSafe>=2.1.2       # Allow any version >= 2.1.2
requests>=2.31.0        # Allow any version >= 2.31.0
six>=1.16.0             # Allow any version >= 1.16.0
soupsieve>=2.4.1        # Allow any version >= 2.4.1
urllib3>=1.26.16        # Allow any version >= 1.26.16
Werkzeug>=2.3.6         # Allow any version >= 2.3.6
pymongo>=4.4.1          # Allow any version >= 4.4.1
